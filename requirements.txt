# Floors, not pins: every one of these is the version that closes a published
# advisory found by the audit job on its first run. Dependabot moves them forward.
fastapi>=0.121.0
uvicorn[standard]>=0.34.0
jinja2>=3.1.6
python-multipart>=0.0.32
requests>=2.33.0
beautifulsoup4>=4.12.3
anthropic>=1.0
starlette>=1.6.0
